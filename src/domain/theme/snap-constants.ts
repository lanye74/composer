// -- Snap guideline token defaults ---------------------------------------------
// Shared so the dark and light snap colors never drift between preset files.

const DARK_SNAP = "#ffd66b";
const LIGHT_SNAP = "#f5a623";

export { DARK_SNAP, LIGHT_SNAP };
