// -- Vocal onset token defaults ------------------------------------------------
// Shared so the dark and light onset colors never drift between preset files.

const DARK_ONSET = "#4fd6c0";
const LIGHT_ONSET = "#2a9d8f";

export { DARK_ONSET, LIGHT_ONSET };
